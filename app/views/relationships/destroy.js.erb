$("#followers-count").html("<%= j(link_to @member.followers.size, member_followers_path(@member)) %>");
$("#followings-count").html("<%= j(link_to current_member.followings.size, member_followers_path(current_member)) %>");
$(".follow-btn").html("<%= j(render 'relationships/follow-btn', member: @member) %>");